<section>
    <img class="asanaImage" src="{{asana.img_url}}" alt="placeholder">
    <div class="asanaInfo">
        <h3 class="title is-3">{{Asana.english_name}} | {{Asana.sanskrit_name}}</h3>
        <h4 class="subtitle is-4">Focus type</h4>
        <h5 class="subtitle is-5">{{focus.name}}</h5>
        <h5 class="subtitle is-4">Difficulty level</h5>
        <h5 class="subtitle is-5">{{asana.difficulty_level}}</h5>
    </div>
</section>